#include "main.h"
#include <stdio.h>

/**
 * mul - multiplies two integers
 * @a: one integer being tested
 * @b: another integer being tested
 * Return: Always 0
 */

int mul(int a, int b)
{
	return (a * b);
}
