#include <stdio.h>

/**
 * main - putchar alphabet in lowercase in lower case
 * Return - Always 0 (Success)
 */

int main (void)
{
	char n = 'a';
	while (n <= 'z')
	{putchar (n);n++;}
	putchar ('\n');
	return (0);
}
		
